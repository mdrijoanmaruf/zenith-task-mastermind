// This file is intentionally left empty to prevent import errors.
// When importing from '@/components/ui', always specify the component file.
// For example: import { Button } from '@/components/ui/button';

// This file exists only to prevent build errors when importing from the directory. 